<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>ZeroAnime</title>
  <meta name="description" content="Watch anime online: popular, trending and new episodes." />
  <link rel="stylesheet" href="/public/css/styles.css" />
  <script>
    (function(){try{var m=localStorage.getItem('theme');if(m==='dark'){document.documentElement.classList.add('dark');}else{document.documentElement.classList.remove('dark');if(!m){localStorage.setItem('theme','light');}}}catch(e){}})();
  </script>
</head>
<body class="app">
  <% if (!['/login', '/register'].includes(path)) { %>
    <header class="topbar <%= path === '/' ? 'is-home' : 'is-inner' %>">
      <div class="logo"><a href="/">Zero<span>Anime</span></a></div>
      <form action="/" method="get" class="searchbar">
        <input type="text" name="q" placeholder="Search anime..." value="<%= q || '' %>" />
      </form>
      <nav class="nav">
        <a href="/forum" class="nav-posts">
          <svg class="icon" viewBox="0 0 24 24" aria-hidden="true">
            <path fill="none" stroke-width="2" d="M6 2h9l5 5v15H6zM15 2v5h5" />
          </svg>
          Посты
        </a>
        <% if (user) { %>
          <a href="/chats">
            <svg class="icon" viewBox="0 0 24 24" aria-hidden="true">
              <path stroke="none" d="M4 5h16v10H7l-3 3z" />
            </svg>
            Чат
          </a>
        <% } %>
        <% if (user && user.is_admin) { %>
          <a href="/admin">Админ панель</a>
          <a href="/admin/anime/new">Добавить аниме</a>
          <a href="/admin/videos">Загрузить видео</a>
          <a href="/admin/support">Техподдержка</a>
        <% } %>
      </nav>
      <div class="user-actions">
        <% if (path !== '/') { %>
          <a href="/" class="btn back-home" title="На главную" aria-label="На главную">
            <svg class="icon" viewBox="0 0 24 24" aria-hidden="true">
              <path fill="none" stroke-width="2" d="M15 18l-6-6 6-6" />
            </svg>
          </a>
        <% } %>
        <button id="themeToggle" class="btn" title="Theme"></button>
        <% if (user) { %>
          <a href="/verify-request" class="btn">Получить галочку <img class="checkmark" src="/public/img/checkmark.png" alt="Галочка" /></a>
          <a href="/user/<%= user.username %>" class="btn user-badge">
            <img class="user-avatar" src="<%= user.avatar_path || '/public/img/placeholder.webp' %>" alt="<%= user.username %>" />
            <span>Профиль</span>
            <% if (user.is_verified) { %>
              <img class="checkmark" src="/public/img/checkmark.png" alt="Галочка" />
            <% } %>
          </a>
          <form action="/logout" method="post" style="display:inline"><button class="btn danger">Logout</button></form>
        <% } else { %>
          <a href="/login" class="btn">Login</a>
          <a href="/register" class="btn primary">Register</a>
        <% } %>
      </div>
    </header>
  <% } %>
  <% if (user) { %>
    <a href="/support" class="support-fab" title="Техподдержка" aria-label="Техподдержка">?</a>
  <% } %>
  <main class="container">
